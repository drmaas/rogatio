# Model selection

Canonical definitions live in [`../shared/models.md`](../shared/models.md).

This file is a redirect stub so older RPI paths (`models.md` next to the skill) still resolve. Always edit the shared file.
